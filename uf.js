class UnionFind {
    constructor(arr) {
        const n = arr.length;
        this.sets = arr.slice();
        this.sizes = new Array(n).fill(1);
    }

    union(x, y) {
        
    }

    find(x) {

    }
}